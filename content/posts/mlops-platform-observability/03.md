+++
date = '2025-10-22T17:10:21+09:00'
draft = false
title = '[MLOps 플랫폼 Observability & Data Pipeline - 3단계 : Prometheus/KSM/Kubelet 완전 분리 구조]'
categories = ['Observability', 'Kubernetes', 'Monitoring Stack', 'Tourble Shoot', 'NFS', 'MLOps Pipeline', 'Prometheus']
+++

## Prometheus/KSM/Kubelet 완전 분리 구조

---

## 🧠 시나리오 설명

> “dev와 prod는 분리돼 있어야 하는데,
> 
> 
> 정작 관측 데이터에서는 서로 섞여 보이는 문제가 계속 생겼습니다.”
> 
> “Prometheus/KSM/Kubelet의 라벨과 셀렉터가 조금만 어긋나도
> 
> dev에서 prod 메트릭을 읽는 심각한 혼선이 발생하기 때문에,
> 
> 이번 단계에서는 관측 스택을 **라벨 기반으로 완전히 절단**하는 데 집중했습니다.”
> 
> “그리고 TSDB는 로컬 디스크로 옮겨 **속도·안정성**을 끌어올리고,
> 
> NFS는 나머지 관측 도구 전용으로 재정비하여
> 
> 이후 Loki·Alertmanager 통합 기반의 Observability 전체 구조를 안정적으로 받칠 수 있도록 만들었습니다.”
> 

---

### 🎯 핵심 요약

- **문제**
    - `prometheus-dev.local`에서 `airflow-prod`, `fastapi-prod` 메트릭이 섞여서 나옴
    - kube-state-metrics / kubelet(cAdvisor) / ServiceMonitor 라벨이 꼬여 dev↔prod 교차 수집
    - NFS 기반 Prometheus TSDB로 **worker2 종료 지연 + I/O 부담** 체감
- **목표**
    - dev/prod **메트릭 수집 완전 분리**
    - Prometheus TSDB를 **local-path로 전환(속도·안정성 개선)**
    - NFS는 관측 도구(Grafana/Alertmanager/로그 등)에만 사용
- **결론**
    - 라벨/selector/metricRelabelings 정리만 해도 dev/prod가 깨끗이 분리됨
    - TSDB를 local-path로 옮기면 **Prometheus는 빠르고 독립적인 로컬 디스크**,
        
        나머지는 NFS로 일관성 있게 관리하는 구조 완성
        

---

## 1️⃣ 문제 상황 정리 — “왜 dev에서 prod가 보이지?”

### 1-1. 실제로 겪은 증상

- `prometheus-dev.local`에서 아래 쿼리를 날리면:

```bash
curl -skG 'https://prometheus-dev.local/api/v1/query' \
  --data-urlencode 'query=count by(namespace)(kube_pod_info)'

```

- 결과에 `airflow-prod`, `fastapi-prod`, `monitoring-prod` 같은 **prod 네임스페이스가 같이 등장**

→ 겉으로 보기엔 스택은 dev/prod로 나눠져 있는데,

관측 데이터 레벨에서는 “섞여 있는 상태”였던 것.

### 1-2. 나중에 알게 된 진짜 원인들

1. `kube-state-metrics.prometheus.monitor.additionalLabels.release` 값을 잘못 넣어서
    - prod 쪽 KSM도 `release=monitoring-dev` 로 찍혀 버림
2. Prometheus의 `serviceMonitorSelector.matchLabels.release` 가 dev/prod 간 일관성이 없음
3. kubelet(cAdvisor) 메트릭에 **namespace 필터가 안 들어가 있어서**
    - 노드 기준으로 떠 있는 모든 컨테이너 메트릭이 섞여 들어옴

---

## 2️⃣ dev/prod 완전 분리 설계 — “라벨로 자르는 기준 만들기”

핵심 개념은 간단합니다.

> “모든 관측 리소스에는 release=monitoring-{env} 라벨을 강제하고,
> 
> 
> Prometheus는 자기 env의 `release` 값만 본다.”
> 

### 2-1. Prometheus 셀렉터 통일

### dev (`apps/monitoring-dev.yaml`)

```yaml
prometheus:
  prometheusSpec:
    serviceMonitorSelector:
      matchLabels:
        release: monitoring-dev
    podMonitorSelector:
      matchLabels:
        release: monitoring-dev
    ruleSelector:
      matchLabels:
        release: monitoring-dev

```

### prod (`apps/monitoring-prod.yaml`)

```yaml
prometheus:
  prometheusSpec:
    serviceMonitorSelector:
      matchLabels:
        release: monitoring-prod
    podMonitorSelector:
      matchLabels:
        release: monitoring-prod
    ruleSelector:
      matchLabels:
        release: monitoring-prod

```

- 이 셋이 통일돼야 합니다:
    - `ServiceMonitor.metadata.labels.release`
    - `PodMonitor.metadata.labels.release`
    - `PrometheusRule.metadata.labels.release`
- dev는 무조건 `monitoring-dev`, prod는 무조건 `monitoring-prod`

---

## 3️⃣ kube-state-metrics (KSM) — dev/prod 라벨 & 필터링

### 3-1. 잘못됐던 부분

```yaml
# (오류) apps/monitoring-prod.yaml
kube-state-metrics:
  prometheus:
    monitor:
      enabled: true
      additionalLabels:
        release: monitoring-dev   # ❌ 여기가 dev로 들어가 있었음

```

→ prod KSM의 ServiceMonitor도 dev Prometheus가 보는 `release=monitoring-dev` 라벨 세트를 타버림.

### 3-2. 수정본 (핵심)

```yaml
# apps/monitoring-prod.yaml
kube-state-metrics:
  prometheus:
    monitor:
      enabled: true
      additionalLabels:
        release: monitoring-prod   # ✅ prod로 수정
      metricRelabelings:
        - action: keep
          sourceLabels: [namespace]
          regex: '.*-prod|monitoring-prod|kube-system'

```

dev도 동일 구조로:

```yaml
# apps/monitoring-dev.yaml
kube-state-metrics:
  prometheus:
    monitor:
      enabled: true
      additionalLabels:
        release: monitoring-dev
      metricRelabelings:
        - action: keep
          sourceLabels: [namespace]
          regex: '.*-dev|monitoring-dev|kube-system'

```

- `additionalLabels.release = monitoring-{env}`
    
    → Prometheus 쪽 `serviceMonitorSelector`와 연결
    
- `metricRelabelings`
    
    → namespace 기준으로 dev/prod/kube-system만 남기기
    

### 3-3. 검증 쿼리

```bash
curl -skG 'https://prometheus-dev.local/api/v1/query' \
  --data-urlencode 'query=count by(namespace)(kube_pod_info)'

curl -skG 'https://prometheus-prod.local/api/v1/query' \
  --data-urlencode 'query=count by(namespace)(kube_pod_info)'

```

- dev: `dev`, `monitoring-dev`, `kube-system` 만 등장
- prod: `prod`, `monitoring-prod`, `kube-system` 만 등장

---

## 4️⃣ kubelet / cAdvisor — “노드는 공유인데, 메트릭도 공유되면 큰일”

### 4-1. 문제 포인트

- kubelet의 `/metrics/cadvisor` 엔드포인트는 **노드 전체 파드** 메트릭을 노출
- dev/prometheus / prod/prometheus 둘 다 kubelet을 스크랩하면,
    - 한쪽에서 다른 쪽 네임스페이스 메트릭까지 같이 보게 됨
- 기본 `metricRelabelings`는 `/metrics`용이고,
    
    `/metrics/cadvisor`에는 **적용되지 않음**
    

### 4-2. 해결: `cAdvisorMetricRelabelings` 추가

dev 예시:

```yaml
kubelet:
  service:
    labels:
      stack: dev           # prod는 stack: prod
  serviceMonitor:
    namespaceSelector:
      matchNames: [kube-system]
    selector:
      matchLabels:
        stack: dev         # dev kubelet만 타게
    metricRelabelings:
      - action: keep
        sourceLabels: [namespace]
        regex: '.*-dev|monitoring-dev|kube-system'
    cAdvisorMetricRelabelings:     # ✅ 여기서 한 번 더 필터
      - action: keep
        sourceLabels: [namespace]
        regex: '.*-dev|monitoring-dev|kube-system'

```

prod는 `stack: prod`, `monitoring-prod`, `.*-prod` 로 동일 구조.

### 4-3. 검증 쿼리

```bash
curl -skG 'https://prometheus-dev.local/api/v1/query' \
  --data-urlencode 'query=count by(namespace)(container_cpu_cfs_periods_total{job="kubelet", __metrics_path__="/metrics/cadvisor"})'

curl -skG 'https://prometheus-prod.local/api/v1/query' \
  --data-urlencode 'query=count by(namespace)(container_cpu_cfs_periods_total{job="kubelet", __metrics_path__="/metrics/cadvisor"})'

```

→ dev 쿼리 결과에 prod namespace가 보이면 아직 어딘가 구멍이 있는 것.

---

## 🧩 팁 — “stale metric & 잡소음 알람 정리”

### 5-1. stale metric 정리

교차 수집 이슈를 해결했어도 **이전에 수집된 시계열**이 잠깐 남아 있을 수 있습니다.

- Prometheus가 자체적으로 처리하지만,
    
    환경을 깨끗하게 리셋하고 싶다면:
    

```bash
kubectl -n monitoring-dev  rollout restart deploy/monitoring-dev-kube-state-metrics
kubectl -n monitoring-prod rollout restart deploy/monitoring-prod-kube-state-metrics
kubectl -n monitoring-dev  rollout restart statefulset prometheus-monitoring-dev-kube-promet-prometheus
kubectl -n monitoring-prod rollout restart statefulset prometheus-monitoring-prod-kube-prome-prometheus

```

### 5-2. tmp-curl CrashLoopBackOff 알람

- 임시 디버깅용 Pod `tmp-curl`을 띄웠다가 CrashLoopBackOff 상태로 두면
    
    `KubePodCrashLooping` 알람이 계속 뜹니다.
    

```bash
kubectl -n monitoring-dev delete pod tmp-curl --force --grace-period=0

```

### 5-3. Watchdog 알람 Slack 소음 제거

기본 Watchdog 알람은 “파이프라인이 살아있는지 확인하는 용도”라 항상 firing 됩니다.

```yaml
route:
  routes:
    - match:
        alertname: Watchdog
      receiver: "null"

receivers:
  - name: "null"

```

→ Slack으로 Watchdog가 날아오지 않게 차단.

---

## 6️⃣ Prometheus TSDB 스토리지 — NFS → local-path 전환

이제 **성능/안정성 쪽 이야기**입니다.

### 6-1. 왜 Prometheus만 local-path로 옮겼나?

- TSDB 특성상 **쓰기·읽기 I/O가 많고 민감함**
- NFS 위에 올리면:
    - worker 노드 종료 시 **unmount 대기 → 종료 지연**
    - 네트워크/서버 레이턴시가 바로 관측 성능에 영향
- Grafana/Alertmanager는 I/O량이 훨씬 적으므로 NFS로도 충분

그래서 구조를 이렇게 정리:

- Prometheus: `local-path` (노드 로컬 디스크)
- Grafana/Alertmanager: NFS(`nfs-monitoring`)

### 6-2. values 수정

### dev

```yaml
prometheus:
  prometheusSpec:
    storageSpec:
      volumeClaimTemplate:
        spec:
          storageClassName: local-path      # 🔁 nfs-monitoring → local-path
          accessModes: ["ReadWriteOnce"]
          resources:
            requests:
              storage: 50Gi

```

prod도 동일 구조.

### 6-3. 실제 전환 절차 (데이터 초기화 전제)

```bash
# (1) 값 반영
kubectl apply -f apps/monitoring-dev.yaml
kubectl apply -f apps/monitoring-prod.yaml

# (2) Prometheus 일시 중단
kubectl -n monitoring-dev  scale statefulset prometheus-monitoring-dev-kube-promet-prometheus --replicas=0
kubectl -n monitoring-prod scale statefulset prometheus-monitoring-prod-kube-prome-prometheus --replicas=0

# (3) 기존 NFS PVC 삭제
kubectl -n monitoring-dev  delete pvc -l app.kubernetes.io/name=prometheus
kubectl -n monitoring-prod delete pvc -l app.kubernetes.io/name=prometheus

# (4) Prometheus 재기동 (이제 local-path 기반 PVC 생성)
kubectl -n monitoring-dev  scale statefulset prometheus-monitoring-dev-kube-promet-prometheus --replicas=1
kubectl -n monitoring-prod scale statefulset prometheus-monitoring-prod-kube-prome-prometheus --replicas=1

```

> 중간에 ArgoCD selfHeal 때문에 레플리카가 다시 올라오지 않도록
> 
> 
> 잠깐 타이밍만 신경 써주면 됩니다.
> 

### 6-4. PVC가 실제로 local-path를 쓰는지 확인

```bash
kubectl -n monitoring-dev  get pvc | grep prometheus
kubectl -n monitoring-prod get pvc | grep prometheus
# STORAGECLASS 열이 local-path 여야 정상

```

---

## 7️⃣ Prometheus TSDB 실제 위치 — “데이터가 디스크 어디에 있나”

### 7-1. PVC → PV → 호스트 경로 추적

```bash
kubectl -n monitoring-dev get pvc <prometheus-pvc-name> -o yaml | grep volumeName
# volumeName: pvc-xxxxxx

kubectl get pv pvc-xxxxxx -o yaml | grep path
# path: /opt/local-path-provisioner/pvc-xxxxxx

```

→ **실제 TSDB 데이터는 노드의 이 경로에 저장됩니다:**

```
/opt/local-path-provisioner/pvc-xxxxxx

```

컨테이너 내부에서는 같은 볼륨이 `/prometheus` 로 마운트되어 있습니다.

### 7-2. 노드에서 직접 확인하고 싶을 때

```bash
# 어느 노드에 올라와 있는지
kubectl -n monitoring-dev get pod -o wide | grep prometheus

# 해당 노드에 SSH 접속 후
sudo du -sh /opt/local-path-provisioner/pvc-xxxxxx
sudo ls /opt/local-path-provisioner/pvc-xxxxxx

```

- `chunks_head`, `wal`, `snapshots` 등 TSDB 구조가 보이면 정상

---

## 8️⃣ NFS는 어디에 쓰나? — 관측 스택 나머지(PVC) 전략

### 8-1. NFS 동적 프로비저너 (observability 전용)

Loki/Promtail, Grafana, Alertmanager 등 **로그/대시보드 계열**은

공유 스토리지가 편해서 NFS를 유지합니다.

```bash
helm upgrade --install nfs-prov \
  nfs-subdir-external-provisioner/nfs-subdir-external-provisioner \
  -n storage \
  --set nfs.server=192.168.18.141 \
  --set nfs.path=/mnt/nfs_share/mlops/observability \
  --set storageClass.name=nfs-observability \
  --set storageClass.defaultClass=false \
  --set storageClass.accessModes=ReadWriteMany

```

→ `nfs-observability` 라는 StorageClass 생성

→ Loki / Grafana / Alertmanager / 기타 로그성 PV에 사용

### 8-2. Monitoring 스택에서 NFS 사용하는 영역

- Grafana: 대시보드/설정
- Alertmanager: 알람 히스토리
- Loki: 로그 보관
- FastAPI/MLflow 로그용 PVC (선택)

Prometheus TSDB는 이미 local-path로 분리했으므로

**읽기/쓰기 많은 시계열은 로컬, 공유가 편한 설정/로그는 NFS** 라는 역할 분리가 됩니다.

---

## 🔧 MLOps 실전 연결

- **실험 환경(dev)과 운영 환경(prod)을 관측 레벨에서 완전히 분리**
    - “dev에서 한 실험이 prod 그래프에 섞여 들어가는” 사고를 미리 차단
- **Prometheus TSDB를 local-path로 분리**
    - 노이즈 많은 환경에서도 **빠른 쿼리 + 안정적인 종료** 확보
- **NFS 기반 observability 스토리지 계층**
    - Loki/Promtail, Grafana, Alertmanager가 공통 스토리지를 쓰면서도
        
        dev/prod 네임스페이스와 라벨로 논리 분리
        

> 이걸 기반으로 다음 단계에서는
> 
> 
> **Loki + Promtail + Alertmanager / Grafana 연동**을 다루면서,
> 
> “로그·메트릭·알람이 하나의 파이프라인에서 어떻게 만나는지”까지 이어가면 좋습니다.
> 

---

## 🏁 정리

| 구분 | 내용 | 상태 |
| --- | --- | --- |
| dev/prod 메트릭 교차 수집 | KSM/ServiceMonitor/Kubelet 라벨 정리 | ✅ 해결 |
| kube-state-metrics 필터링 | `metricRelabelings`로 namespace 필터 | ✅ 적용 |
| kubelet(cAdvisor) 교차 수집 | `cAdvisorMetricRelabelings` 추가 | ✅ 분리 |
| stale metric | Prometheus & KSM 재시작 | ✅ 정리 |
| 임시 Pod CrashLoop 알람 | `tmp-curl` 삭제 | ✅ 소음 제거 |
| Watchdog Slack 소음 | `null` receiver로 분기 | ✅ 차단 |
| Prometheus 스토리지 | NFS → local-path 전환 | ✅ 성능/안정성 개선 |
| TSDB 위치 파악 | `/opt/local-path-provisioner/pvc-*` | ✅ 구조 이해 |

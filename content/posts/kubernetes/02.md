+++
date = '2025-06-05T20:06:38+09:00'
draft = false
title = '[Kubernetes - 2단계: ConfigMap & Secret]'
categories = 'Kubernetes'
+++

## 이 글에서 다루는 것

> 애플리케이션 설정값(ConfigMap)과 민감 정보(Secret)를 코드에서 분리하여 Kubernetes 리소스로 관리하는 방법을 다룹니다.

## 선수지식

- [Kubernetes 1단계: Deployment & Service](/mlops-journey/posts/kubernetes/01/) — Pod 배포와 Service 기본 개념

---

## 이 단계에서 해결하려는 문제

애플리케이션 코드에 DB 비밀번호나 환경 설정을 하드코딩하면, 환경별 배포가 어렵고 보안 위험이 생깁니다. Kubernetes의 ConfigMap과 Secret을 사용하면 설정과 코드를 분리하고, 환경 변수로 Pod에 주입할 수 있습니다.

실습 코드: [GitHub (Configmap_and_Secret)](https://github.com/keonhoban/mlops-infra-labs/tree/main/k8s-basic/02_Configmap_and_Secret)

---

## 🧭 전체 흐름 요약

```
① ConfigMap 생성 (일반 설정값)
② Secret 생성 (민감 정보 – base64 인코딩)
③ Pod에 환경 변수로 주입
④ 컨테이너 안에서 값 확인 (env 명령어)
⑤ 실전 적용 및 보안 주의사항 학습
```

---

## 📂 실습에 사용된 파일 구성

```
k8s-configmap-secret/
├── configmap.yaml     # 일반 설정값 정의
├── secret.yaml        # 민감 정보 (base64 인코딩)
├── pod-env.yaml       # Pod 환경 변수로 주입
└── README.md
```

---

## 📄 주요 YAML 파일 정리

### `configmap.yaml`

```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  APP_MODE: "production"
  APP_PORT: "8080"
```

---

### `secret.yaml`

```yaml
apiVersion: v1
kind: Secret
metadata:
  name: app-secret
type: Opaque
data:
  DB_PASSWORD: c3VwZXJzZWNyZXQ=   # base64 → "supersecret"
```

> 인코딩 방법: `echo -n "supersecret" | base64`

---

### `pod-env.yaml`

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: env-demo
spec:
  containers:
  - name: demo-container
    image: busybox
    command: [ "sh", "-c", "env; sleep 3600" ]
    env:
    - name: APP_MODE
      valueFrom:
        configMapKeyRef:
          name: app-config
          key: APP_MODE
    - name: APP_PORT
      valueFrom:
        configMapKeyRef:
          name: app-config
          key: APP_PORT
    - name: DB_PASSWORD
      valueFrom:
        secretKeyRef:
          name: app-secret
          key: DB_PASSWORD
```

---

## 🧪 실습 실행 및 검증

```bash
kubectl apply -f configmap.yaml
kubectl apply -f secret.yaml
kubectl apply -f pod-env.yaml

kubectl get pods
kubectl describe pod env-demo
kubectl exec -it env-demo -- sh
```

```bash
# 컨테이너 내부에서 확인
env

# 출력 예시 (중간 생략)
APP_MODE=production
APP_PORT=8080
DB_PASSWORD=supersecret
```

---

## 🎯 정리 요약

| 구분 | 설명 |
| --- | --- |
| ConfigMap | 비민감 설정값 저장 (APP_MODE, APP_PORT 등) |
| Secret | 민감한 데이터 저장 (비밀번호, API 키 등) – base64 인코딩 |
| 주입 방식 | `env`를 통해 컨테이너에 주입 |
| 보안 주의 | Secret은 인코딩일 뿐, RBAC 및 접근 제어 필수 |

---

## 설계 판단 (Why This Way?)

ConfigMap은 일반 설정, Secret은 민감 정보를 분리 관리하되, Secret의 base64는 암호화가 아니므로 프로덕션에서는 SealedSecrets이나 Vault 같은 추가 보안 조치가 필수입니다.

---

## 다음에 읽을 글

→ [Kubernetes 3단계: Ingress & Nginx Controller](/mlops-journey/posts/kubernetes/03/) — 외부 트래픽 라우팅 구성
